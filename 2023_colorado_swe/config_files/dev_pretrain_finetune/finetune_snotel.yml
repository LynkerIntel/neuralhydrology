# --- Experiment configurations --------------------------------------------------------------------

# experiment name, used as folder name
experiment_name: finetune_hs64_lossMSE_lr1e-3_HA_snotel_seed5

# files to specify training, validation and test basins (relative to code root or absolute path)
train_basin_file: ./basin_txt_files/co_basins.txt
validation_basin_file: ./basin_txt_files/co_basins.txt
test_basin_file: ./basin_txt_files/co_basins.txt

# --- Training configuration -----------------------------------------------------------------------

# specify learning rates to use starting at specific epochs (0 is the initial learning rate)
learning_rate:
  0: 1e-3
  1: 5e-4
  2: 1e-4
  3: 5e-5

# Number of training epochs
epochs: 10

finetune_modules:
- head
- lstm
base_run_dir: /home/jonat/nh-lyker/2023_colorado_swe/runs/pretrain_hs64_lossMSE_lr1e-3down_HA_snotel_seed5_0804_143004